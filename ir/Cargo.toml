[package]
name = "ir"
version = "0.1.0"
edition = "2024"

[dependencies]
smol_str.workspace = true
arena.workspace = true
arena-scope.workspace = true
annotate-snippets = { version = "0.12.15", optional = true }

[features]
format = ["dep:annotate-snippets"]
default = ["format"]
