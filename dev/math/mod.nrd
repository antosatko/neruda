type Im<S> = (S, S)

type A<a> = struct {}

type ImFloat = Im<f32>
type ImInt = Im<i32>

type eee = enum { sdfgsdf }

const danda: i32 = 60

const danda2: i32 = danda + danda;

// [docs] method [generic_params] type . method_identifier params [return_type] (block)

// method ImFloat.add(&self, other: Self): Self => (
//     self.0 + other.0,
//     self.1 + other.1,
// )
